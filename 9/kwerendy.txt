1. SELECT klienci.imie, klienci.kolor, klienci.cena FROM klienci WHERE topKlient = 1;
2. SELECT klienci.imie, klienci.cena FROM klienci WHERE termin LIKE '2024-05-23%';
3. SELECT klienci.imie, klienci.kolor, ksztalty.ksztalt FROM klienci 
   JOIN ksztalty ON klienci.ksztalty_id = ksztalty.id WHERE ksztalty.ksztalt LIKE 'migdał';
4. ALTER TABLE wzory ADD COLUMN dopłata INT;

